# Carpeta de datos

Coloque aquí el archivo Excel oficial del ONSV con el siguiente nombre exacto:

`BBDD ONSV - SINIESTROS FATALES 2021-2025 (preliminar).xlsx`

La aplicación y el script de entrenamiento leerán la hoja `SINIESTROS` desde esa ruta.
